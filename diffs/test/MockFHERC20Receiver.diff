# contracts/test/MockFHERC20Receiver.sol  ->  contracts/test/MockFHERC20Receiver.fsol
# upstream 5138cb8 .. ported 4cefd22

diff --git a/contracts/test/MockFHERC20Receiver.sol b/contracts/test/MockFHERC20Receiver.fsol
index 33e9575..6b8bcaa 100644
--- a/contracts/test/MockFHERC20Receiver.sol
+++ b/contracts/test/MockFHERC20Receiver.fsol
@@ -1,7 +1,7 @@
 // SPDX-License-Identifier: MIT
 pragma solidity ^0.8.25;
 
-import { IERC7984Receiver } from "../interfaces/IERC7984Receiver.sol";
+import { IERC7984Receiver } from "../interfaces/IERC7984Receiver.fsol";
 import { ebool, sharedEbool, sharedEuint64, FHE } from "@fhenixprotocol/cofhe-contracts/FHE.sol";
 
 contract MockFHERC20Receiver is IERC7984Receiver {
@@ -16,7 +16,7 @@ contract MockFHERC20Receiver is IERC7984Receiver {
         address,
         sharedEuint64,
         bytes calldata data
-    ) external returns (sharedEbool) {
+    ) external returns (shared(msg.sender) ebool) {
         uint8 input = abi.decode(data, (uint8));
 
         if (input > 1) revert InvalidInput(input);
@@ -24,9 +24,9 @@ contract MockFHERC20Receiver is IERC7984Receiver {
         bool success = input == 1;
         emit ConfidentialTransferCallback(success);
 
-        ebool returnVal = FHE.asEbool(success);
+        ebool returnVal = ebool(success);
 
         // Directed back at the token that called us, instead of an out-of-band grant.
-        return FHE.shareEbool(returnVal, msg.sender);
+        return returnVal;
     }
 }
